language: C

addons:
  apt:
    packages:
    - autoconf
    - pkg-config
    - libcurl4-openssl-dev
    - libjansson-dev
    - libev-dev
    - libboost-all-dev
    - build-essential
    - libtool
    - autoconf 
    - jq

install: true

script:
  - make full
